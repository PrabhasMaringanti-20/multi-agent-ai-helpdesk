# Development + testing dependencies (mirrors [project.optional-dependencies].dev).
# Install runtime deps first, then these:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
types-passlib>=1.7
