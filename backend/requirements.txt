# Runtime dependencies for the Enterprise AI Helpdesk backend.
# Mirrors [project].dependencies in pyproject.toml (the authoritative source).
# Beginner install:  pip install -r requirements.txt
# Dev/test extras:   pip install -r requirements-dev.txt

# ---- Web framework ----
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ---- Config / validation ----
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1

# ---- Database ----
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29
psycopg[binary]>=3.2
alembic>=1.13

# ---- Cache / broker ----
redis>=5.0
celery[redis]>=5.4

# ---- Security ----
PyJWT>=2.8
passlib[argon2]>=1.7.4
bcrypt>=4.1

# ---- AI / RAG ----
langgraph>=0.2
langgraph-checkpoint-postgres>=1.0
google-genai>=1.0,<3.0
chromadb>=0.5

# ---- Document ingestion (PDF / DOCX / XLSX) ----
pypdf>=4.2
python-docx>=1.1
openpyxl>=3.1

# ---- HTTP / resilience / TLS ----
httpx>=0.27
truststore>=0.9
