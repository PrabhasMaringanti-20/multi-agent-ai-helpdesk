# Frontend environment (Vite). Values are inlined at build time.
# Same-origin default; the dev server / nginx proxies /api to the backend.
VITE_API_BASE_URL=/api/v1

# Dev-only: where `vite dev` proxies /api to (see vite.config.ts).
VITE_API_PROXY=http://localhost:8000
