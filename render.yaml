services:
  - type: web
    name: ai-helpdesk-api
    runtime: docker
    region: oregon
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: helpdesk-db
          property: connectionString
      - key: POSTGRES_HOST
        fromDatabase:
          name: helpdesk-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: helpdesk-db
          property: port
      - key: POSTGRES_USER
        fromDatabase:
          name: helpdesk-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: helpdesk-db
          property: password
      - key: POSTGRES_DB
        fromDatabase:
          name: helpdesk-db
          property: database
      - key: SECRET_KEY
        generateValue: true
      - key: GEMINI_API_KEY
        sync: false
      - key: LLM_PROVIDER
        value: fake
      - key: EMBEDDING_PROVIDER
        value: fake
      - key: REDIS_URL
        value: ""
      - key: CELERY_BROKER_URL
        value: ""
      - key: CELERY_RESULT_BACKEND
        value: ""
      - key: CORS_ORIGINS
        value: "*"
      - key: APP_ENV
        value: production

databases:
  - name: helpdesk-db
    plan: free
    region: oregon
